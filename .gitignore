node_modules/
.next/
.env
.env.local
dist/
*.log
*.tsbuildinfo
.DS_Store

# Local service state. Bind-mounted from docker-compose.yml.
# Do NOT commit the contents of these directories — they hold
# database files (MongoDB) and LocalStack state (S3 objects).
data/

